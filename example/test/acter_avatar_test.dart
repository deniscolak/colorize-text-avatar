// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

// import 'package:acter_avatar/acter_avatar.dart';
// import 'package:example/constants/keys.dart';
// import 'package:flutter/material.dart';
// import 'package:flutter/rendering.dart';
// import 'package:flutter_test/flutter_test.dart';

// void main() {
//   group('Avatars render test', () {
//     testWidgets('User Circle Avatar with specified size',
//         (WidgetTester tester) async {
//       await tester.pumpWidget(
        
//       );
//       final RenderConstrainedBox box =
//           tester.renderObject(find.byKey(widgetKey));
//       expect(box.size, equals(14));
//       Finder text = find.byKey(TestKeys.);
//     });
//     testWidgets('User Circle Avatar with unspecified size',
//         (WidgetTester tester) async {
//       await tester.pumpWidget(
//         ActerAvatar(
//           key: widgetKey,
//           uniqueId: UniqueKey().toString(),
//           mode: DisplayMode.User,
//         ),
//       );
//       final RenderConstrainedBox box =
//           tester.renderObject(find.byKey(widgetKey));
//       // should expect acter avatar default size
//       expect(box.size, equals(24));
//     });
//   });
// }
